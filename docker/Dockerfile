# Use an appropriate base image for your language and dependencies
FROM python:3.9

# Copy the application code into the container
COPY src/ /app

# Add any other dependencies and setup commands

# Run your application
CMD ["python", "/app/app.py"]
